#+TITLE: Schmidt Lexicon Citations vs. the Globe<->TLN Map: Resolution Analysis
#+DATE: 2026-08-18
#+AUTHOR: Clifford Wulfman
#+OPTIONS: toc:t num:nil

* Overview

=corpus-tools= recently gained =tln_globe_map.py=, which builds a
corpus-wide Globe act.scene.line <-> Folger Through-Line-Number (TLN)
map from ShakeDraCor's 37 Folger-derived Shakespeare TEI files (commit
=4467d7c=). Before relying on it, we exercised it against a large,
independent real-world citation set: Alexander Schmidt's *Shakespeare
Lexicon and Quotation Dictionary* (1902), digitized at
=canonical-engLit/data/schmidt/lexicon/lexicon.xml=. Schmidt cites every
headword occurrence as =<bibl n="shak. {siglum}
{act}.{scene}.{line}">{display text}</bibl>=, using the same lowercase
play sigla and dotted act.scene.line format the map already keys on —
good alignment by construction, and a much larger, messier test than
the hand-built fixtures in =test_tln_globe_map.py=.

The map held up well: *94.37% of clean act.scene.line citations resolve
to a TLN*. This report is about the remaining 5.63% — three genuinely
distinct causes were isolated and verified to different degrees of
confidence, plus one baseline residual and one unrelated ShakeDraCor
data bug found along the way. None of the causes are defects in
=tln_globe_map.py= itself; all trace to how Schmidt's =@n= attribute was
generated, or to gaps/typos in ShakeDraCor's own source.

A regression test (=corpus-tools/tests/test_schmidt_lexicon_map.py=)
now guards the 94% resolution rate and documents one concrete instance
of the baseline residual gap.

** Summary

| Stage                                                    |   Count |    % of citations |
|-----------------------------------------------------------+---------+--------------------|
| Total =shak.= citations in the lexicon                    | 204,318 |               100% |
| Non-dramatic sigla (Sonnets, /Venus and Adonis/, etc.)     |  15,858 |              7.76% |
| Non-clean format (ranges, "sq.", act-only, etc.)           |  10,245 |              5.02% |
| *Clean act.scene.line citations attempted*                 | *178,215* |          *87.22%* |
|-----------------------------------------------------------+---------+--------------------|
| Resolved to a TLN                                          | 168,177 |    94.37% of clean |
| *Unresolved*                                                | *10,038* |  *5.63% of clean*  |

| Unresolved breakdown                                       |  Count | % of unresolved | Diagnosis        |
|--------------------------------------------------------------+--------+------------------+-------------------|
| Single-scene-Act collapse bug                                 |  2,486 |            24.8% | Fully diagnosed   |
| Bare "N, N" display pattern (all plays; Lear subset verified) |  1,623 |            16.2% | Partially verified |
| Other (isolated TLN gaps, prose drift, unverified)             |  5,929 |            59.1% | Partially diagnosed |

* Cause 1: single-scene-Act citation collapse (2,486 citations, fully diagnosed)

When an Act has only one scene — Tempest Act 5, Merchant of Venice Act
5, Measure for Measure Act 5, and others — Schmidt's own printed
citation naturally collapses to just "Act, Line" (there's no scene to
disambiguate). His display text confirms this directly:

#+begin_example
<bibl n="shak. tmp 5.169.458">V, 169</bibl>
<bibl n="shak. mm 5.252.26">V, 252</bibl>
<bibl n="shak. mv 5.147.88">V, 147</bibl>
#+end_example

But whatever tool generated the machine-readable =@n= attribute on
these =<bibl>= elements still forced a three-slot act.scene.line
template onto a two-component "Act, Line" citation. It dropped the real
line number into the *scene* slot and fabricated a third number from
nowhere (=458=, =19=, =1=... no discernible relationship to the real
line). The result: a well-formed-looking three-part reference
(=5.169.458=) that can never resolve, because Tempest's Act 5 has
exactly one scene and no line 169 in it.

*Detection rule* (used to produce the 2,486 count): the citation's Act,
per ShakeDraCor's own =<div type="scene">= structure for that play, has
exactly one scene, and Schmidt's display text is a bare "Roman numeral,
number" pair.

This single bug is the leading cause of Tempest's below-average
resolution rate (86.3%) and contributes to several other plays whose
final act is a single long scene.

* Cause 2: King Lear's continuous through-scene citation (verified 15/15 on a sample; scope beyond Lear unverified)

A chunk of Lear's failures have display text like =22, 12=, =4, 364=,
=1, 4= — a bare pair of arabic numbers, no act indicator at all. Lear
is well known among editors for having no act divisions in its Quarto
source text; the five-act structure now standard was imposed later, and
older editions (including, evidently, Schmidt's Globe source)
frequently cited Lear by a *continuous scene number running straight
through all five acts* (1 through 25) rather than by act.scene.

To verify this, I built Lear's continuous scene order directly from
ShakeDraCor's own document order (each =<div type="scene">= in
document order, regardless of its parent Act), then reinterpreted each
sampled bare "N, line" display pair as (continuous-scene, line) instead
of Schmidt's mis-encoded (act, scene, line):

#+begin_example
continuous scene 22 = Act 4, Scene 7   (ShakeDraCor skips a numbered
                                         "Scene 4" within this Act, so
                                         continuous count and per-act
                                         scene count diverge)
"22, 12"  ->  Act 4 Scene 7, line 12  ->  globe "4.7.12"  ->  TLN 3060  [correct]
"4, 364"  ->  Act 1 Scene 4, line 364 ->  globe "1.4.364" ->  TLN 940   [correct]
#+end_example

All 15 sampled citations resolved correctly under this reinterpretation
(100%). This mechanism accounts for at least 74 of Lear's 277
unresolved citations (the ones with this exact bare-numeric display
shape).

*Important caveat — do not over-generalize this fix.* The same bare
"N, N" display shape recurs at similar magnitude in essentially *every*
play in the corpus, not just Lear — 1,623 instances corpus-wide,
including plays with no Quarto/act-division history at all (Cymbeline
121, Antony and Cleopatra 103, Hamlet 96, Othello 94). Most of these
plays have no scholarly reason to use continuous scene numbering, so
this shared display shape almost certainly has more than one
underlying cause. The leading unverified hypothesis is that many of
these are Schmidt's *internal* cross-references within his own
dictionary (e.g. "see p. 85", rendered the same way a play citation
would be) that got swept up by the =@n=-generating tool as if they were
play citations. This has *not* been verified and should not be treated
as understood — it needs its own investigation, play by play, before
any repair pass assumes Lear's fix generalizes.

Pericles is a plausible second candidate for genuine continuous-scene
citation (it shares Lear's Quarto/act-division problem — the Quarto has
no act divisions), but this has not been separately verified the way
Lear was.

* Cause 3: 2 Henry IV's prose relineation drift (partially diagnosed)

2 Henry IV has the worst resolution rate in the corpus, 82.53% (927
unresolved out of 5,306 clean citations), and by a wide margin: the
next-worst play (Tempest) sits at 86.3%. It also has the highest
prose fraction of any play in the corpus — 51.8% of ShakeDraCor's
through-lines are =<lb>= prose milestones rather than =<l>= verse
lines, against a corpus median well under half that.

Prose line breaks are a typesetting artifact of a given edition's page
— where the line wraps depends on page width, font, and the
compositor's choices — not fixed by the poet the way verse lineation
is. Two different editions of the same prose passage will not, in
general, produce the same line count, and small per-line differences
compound across a long prose scene.

This was confirmed concretely for Act 1 Scene 2 (entirely prose —
Falstaff and his page, then Falstaff's soliloquy on his gout):
ShakeDraCor's own scene ends at =1.2.256=, but Schmidt cites lines in
that scene as high as =1.2.278= — Globe's relineation runs the scene
about 22 lines "longer" than Folger's.

*This mechanism is not fully proven at the scale needed to explain
2H4's whole shortfall.* Classifying each unresolved citation as either
"beyond the end of ShakeDraCor's scene" (clear relineation overflow) or
"in range but still missing" (an isolated gap within the scene) gives:

| Type                                    | 2H4 count | % of 2H4 unresolved |
|-------------------------------------------+-----------+-----------------------|
| Overflow past ShakeDraCor's scene end      |       185 |                 20.0% |
| In-range gap                                |       742 |                 80.0% |

Only the 185 overflow cases are directly and unambiguously explained by
the relineation-drift mechanism as demonstrated above. The remaining
742 in-range gaps are consistent with the same underlying cause
(relineation drift doesn't only produce a longer tail — it can also
produce collisions *within* a scene if the two editions' line breaks
diverge and then partially re-converge), but that has not been verified
line-by-line the way the Lear and single-scene-Act mechanisms were.
Prose relineation remains the leading hypothesis for 2H4, not a closed
case.

* Baseline residual: isolated TLN gaps in ShakeDraCor's own source (all plays, ~1-4%)

Independent of Schmidt entirely, every play in the ShakeDraCor corpus
has a small number of Through-Line-Numbers that simply don't exist
anywhere in that play's TEI file — not missing an =@n= attribute, the
=xml:id= itself is absent. Example: Merry Wives of Windsor jumps
directly from

#+begin_example
<l xml:id="ftln-2307" n="4.4.8">Than thee with wantonness. ...</l>
<l xml:id="ftln-2309" n="4.4.10">In him that was of late an heretic, </l>
#+end_example

=ftln-2308= (which would be =n="4.4.9"=) never appears anywhere in the
file. Schmidt happens to cite =shak. wiv 4.4.9=, which can never
resolve until ShakeDraCor's source is corrected upstream. This pattern
recurs across the whole corpus at roughly 0.3%-4.4% of each play's
lines (lowest in Merry Wives itself and Henry VI Part 1; highest in
Comedy of Errors, Lear, and Macbeth), and reads as genuine editorial
practice — a Through-Line-Number slot reserved but not printed in this
edition — rather than a ShakeDraCor extraction bug. This is the
baseline every play sits on top of, on top of which Causes 1-3 above
add their own, larger effects for specific plays.

=corpus-tools/tests/test_schmidt_lexicon_map.py::test_wiv_4_4_9_is_a_known_source_gap=
documents this exact case as a regression fixture, so it doesn't get
mistaken for a map bug in a future session.

* Aside: a ShakeDraCor data bug in Othello, unrelated to Schmidt

While tracing TLN allocation gaps corpus-wide, two corrupted
=xml:id=\/=n= pairs turned up in ShakeDraCor's own
=tei/othello.xml=, sitting among an otherwise clean, sequential
run of ids in the 3600s:

#+begin_example
<l xml:id="ftln-23830" n="4.1.3"  part="F">What, </l>
<l xml:id="ftln-24080" n="4.1.29" part="F">What </l>
#+end_example

=23830= and =24080= are almost certainly typos for =2383= and =2408=
(an extra trailing zero) — Othello's ids otherwise top out around
3681. Because these ids are numerically distinct from every other TLN
in the file, =build_corpus_map= didn't raise its duplicate-key check
and the bug passed silently. It doesn't affect Globe-ref lookups (the
=n= values =4.1.3=/=4.1.29= are fine) and it isn't something Schmidt
cites into, so it's noted here rather than fixed — it would need to be
corrected upstream in ShakeDraCor's source, or worked around locally if
we ever depend on Othello's TLN values specifically.

* Next step

Plan a Schmidt-citation-repair pass that:
1. Mechanically fixes Cause 1 (single-scene-Act collapse) using the
   structural detection rule above — deterministic, no ambiguity.
2. Applies Lear's continuous-scene reinterpretation, and investigates
   Pericles as a probable second case, /before/ touching the bare "N,
   N" pattern in any other play.
3. Leaves Cause 3 (2H4 prose drift) and the un-generalized part of
   Cause 2 for further investigation rather than guessing — a repair
   that silently "fixes" an unverified case risks producing a
   confidently wrong citation, which is worse than an honest failure to
   resolve.
